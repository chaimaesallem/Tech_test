// src/app/models/service.model.ts
export class Appointement {
    id!: number;
    name!: string;
    date!: string; // Format de date (ex: 'YYYY-MM-DD')
    time!: string; // Format de temps (ex: 'HH:mm')
}